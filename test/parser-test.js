const fs = require('fs');
const path = require("path");
var assert = require('assert');
var parse = require('../parser');

describe('Parser', function() {

    it('testing', function(){
        assert(true);
    });
});